# 오사카 날씨 표시 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 상단 요약에 오늘 날씨, 일차 카드에 그날 예보를 표시한다 (Open-Meteo, 오프라인 시 마지막 예보 + 시각 스탬프).

**Architecture:** 순수 함수(코드 변환·포맷) → fetch+캐시+재렌더 통합 순. 예보는 `store` 키 `weather`에 저장, `data.js` 불변. 기존 렌더 함수에 한 줄씩 끼워 넣는 방식.

**Tech Stack:** 바닐라 JS(기존 var/const 혼용 스타일), Open-Meteo API, localStorage, bundle.py.

## Global Constraints

- API URL은 아래 `WX_URL` 문자열 그대로. 좌표 34.69/135.5, `timezone=Asia%2FTokyo`, `forecast_days=7`.
- WMO 코드 매핑: 0 ☀️맑음 / 1-2 🌤️구름 조금 / 3 ☁️흐림 / 45·48 🌫️안개 / 51-57 🌦️이슬비 / 61-67 🌧️비 / 71-77·85-86 🌨️눈 / 80-82 🌧️소나기 / 95이상 ⛈️뇌우 / 그 외 🌡️(라벨 빈 문자열).
- 표시 포맷: `{이모지} {tmax}° / {tmin}°` + 강수확률 있으면 ` · 비 {n}%`. 상단은 이모지 뒤에 `오늘 ` 접두어.
- 예보 없는 날짜·캐시 없음 → 빈 문자열 (에러 문구 금지).
- fetch 실패 시 캐시 표시 + ` (M/D HH:MM 기준)` 스탬프. 성공 시 스탬프 없음.
- `data.js` 수정 금지. `store` 키는 `weather` 하나만 추가.
- sw.js 캐시 버전 v6 → v7. fetch 핸들러의 `caches.open(CACHE).then(c.put(...))`이 cross-origin(api.open-meteo.com) 응답까지 캐시에 넣지 않도록 same-origin 가드 추가.
- 커밋: `git -c user.name="Pcallpang" -c user.email="ljh6479z@naver.com" commit`
- 작업 디렉터리 `C:\Pcall\Trip`. 테스트는 `test.html`(순수 함수) + Node 스텁.

---

### Task 1: 날씨 순수 함수 + 테스트

**Files:**
- Modify: `C:\Pcall\Trip\app.js` (todayLocal 함수 정의 직후에 삽입)
- Modify: `C:\Pcall\Trip\test.html` (단언 추가)

**Interfaces:**
- Consumes: 없음 (순수 함수만)
- Produces:
  - `WX_URL: string`
  - `wxIcon(code:number): {e:string, t:string}`
  - `wxDailyMap(api): {[date:string]: {code:number, tmax:number, tmin:number, rain:number|null}}` — api가 null/daily 없음이면 `{}`
  - `wxLine(map, date:string): string` — 없는 날짜면 `""`

- [ ] **Step 1: test.html에 실패하는 단언 추가**

`test.html`의 `eq('분류 목록', ...)` 줄 다음에 삽입:

```js
eq('wxIcon 맑음', wxIcon(0), { e: "☀️", t: "맑음" });
eq('wxIcon 구름', wxIcon(2), { e: "🌤️", t: "구름 조금" });
eq('wxIcon 흐림', wxIcon(3), { e: "☁️", t: "흐림" });
eq('wxIcon 이슬비', wxIcon(55), { e: "🌦️", t: "이슬비" });
eq('wxIcon 비', wxIcon(63), { e: "🌧️", t: "비" });
eq('wxIcon 소나기', wxIcon(81), { e: "🌧️", t: "소나기" });
eq('wxIcon 뇌우', wxIcon(96), { e: "⛈️", t: "뇌우" });
eq('wxIcon 미지 코드', wxIcon(42), { e: "🌡️", t: "" });

var WXAPI = { daily: {
  time: ["2026-07-28", "2026-07-29"],
  weather_code: [1, 61],
  temperature_2m_max: [33.4, 29.6],
  temperature_2m_min: [26.2, 25.5],
  precipitation_probability_max: [10, null]
} };
eq('wxDailyMap 변환', wxDailyMap(WXAPI)["2026-07-28"],
  { code: 1, tmax: 33, tmin: 26, rain: 10 });
eq('wxDailyMap null api', wxDailyMap(null), {});
eq('wxLine 포맷', wxLine(wxDailyMap(WXAPI), "2026-07-28"), "🌤️ 33° / 26° · 비 10%");
eq('wxLine 강수 null 생략', wxLine(wxDailyMap(WXAPI), "2026-07-29"), "🌧️ 30° / 26°");
eq('wxLine 없는 날짜', wxLine(wxDailyMap(WXAPI), "2026-08-09"), "");
```

- [ ] **Step 2: 실패 확인**

```bash
cd /c/Pcall/Trip && node -e "
global.localStorage={getItem:()=>null,setItem:()=>{}};
global.document={addEventListener:()=>{}};
eval(require('fs').readFileSync('app.js','utf8'));
console.log(typeof wxIcon);
"
```
Expected: `undefined`

- [ ] **Step 3: 구현**

`app.js`의 `todayLocal()` 함수 닫는 `}` 다음에 삽입:

```js

var WX_URL = "https://api.open-meteo.com/v1/forecast?latitude=34.69&longitude=135.5" +
  "&daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max" +
  "&timezone=Asia%2FTokyo&forecast_days=7";

function wxIcon(code) {
  if (code === 0) return { e: "☀️", t: "맑음" };
  if (code === 1 || code === 2) return { e: "🌤️", t: "구름 조금" };
  if (code === 3) return { e: "☁️", t: "흐림" };
  if (code === 45 || code === 48) return { e: "🌫️", t: "안개" };
  if (code >= 51 && code <= 57) return { e: "🌦️", t: "이슬비" };
  if (code >= 61 && code <= 67) return { e: "🌧️", t: "비" };
  if ((code >= 71 && code <= 77) || code === 85 || code === 86) return { e: "🌨️", t: "눈" };
  if (code >= 80 && code <= 82) return { e: "🌧️", t: "소나기" };
  if (code >= 95) return { e: "⛈️", t: "뇌우" };
  return { e: "🌡️", t: "" };
}
function wxDailyMap(api) {
  var m = {};
  if (!api || !api.daily || !api.daily.time) return m;
  api.daily.time.forEach(function (d, i) {
    var rain = api.daily.precipitation_probability_max[i];
    m[d] = {
      code: api.daily.weather_code[i],
      tmax: Math.round(api.daily.temperature_2m_max[i]),
      tmin: Math.round(api.daily.temperature_2m_min[i]),
      rain: (rain === null || rain === undefined) ? null : rain
    };
  });
  return m;
}
function wxLine(map, date) {
  var w = map[date];
  if (!w) return "";
  var rain = w.rain === null ? "" : " · 비 " + w.rain + "%";
  return wxIcon(w.code).e + " " + w.tmax + "° / " + w.tmin + "°" + rain;
}
```

- [ ] **Step 4: 통과 확인**

Step 2와 같은 Node 스텁에서 test.html의 13개 신규 케이스와 동일한 검사를 실행해 전부 통과 확인. 기존 케이스(spendTotalJpy 등)도 재확인.

- [ ] **Step 5: 커밋**

```bash
git add app.js test.html
git commit -m "feat: 날씨 코드 변환·포맷 순수 함수 추가"
```

---

### Task 2: fetch·캐시·렌더 통합 + 스타일

**Files:**
- Modify: `C:\Pcall\Trip\app.js` (wxState/wxStamp/wxRefresh 추가, renderSummary·renderTimeline·selectDay·init 수정)
- Modify: `C:\Pcall\Trip\styles.css` (끝에 추가)

**Interfaces:**
- Consumes: Task 1의 `WX_URL`, `wxDailyMap`, `wxLine`; 기존 `store`, `todayLocal()`, `renderSummary(meta)`, `renderTimeline(day)`, `selectDay(n)`
- Produces: `wxState = {map, at, live}`, `wxStamp(): string`, `wxRefresh(): void`, `currentDayN: number`

- [ ] **Step 1: 상태·fetch 추가**

`app.js`의 `wxLine` 함수 닫는 `}` 다음에 삽입:

```js

var wxState = { map: {}, at: null, live: false };
var currentDayN = null;

function wxStamp() {
  if (wxState.live || !wxState.at) return "";
  var d = new Date(wxState.at);
  var hh = ("0" + d.getHours()).slice(-2);
  var mm = ("0" + d.getMinutes()).slice(-2);
  return ' <span class="wxstamp">(' + (d.getMonth() + 1) + "/" + d.getDate() +
    " " + hh + ":" + mm + " 기준)</span>";
}
function wxRefresh() {
  var cached = store.get("weather", null);
  if (cached && cached.api) {
    wxState.map = wxDailyMap(cached.api);
    wxState.at = cached.at;
  }
  fetch(WX_URL).then(function (r) { return r.json(); }).then(function (api) {
    store.set("weather", { at: new Date().toISOString(), api: api });
    wxState.map = wxDailyMap(api);
    wxState.at = null;
    wxState.live = true;
    wxRepaint();
  }).catch(function () {
    if (wxState.at) wxRepaint();
  });
}
function wxRepaint() {
  renderSummary(window.TRIP.meta);
  if (currentDayN !== null) selectDay(currentDayN);
}
```

- [ ] **Step 2: renderSummary에 오늘 날씨 줄**

`renderSummary` 안 `'<div class="hotel">🏨 ' + meta.hotel + '</div>' +` 줄 다음에 삽입:

```js
    (function () {
      var line = wxLine(wxState.map, todayLocal());
      return line ? '<div class="wx">' + line.replace(" ", " 오늘 ") + wxStamp() + '</div>' : '';
    })() +
```

주의: `line.replace(" ", " 오늘 ")`는 첫 공백(이모지 뒤)만 치환한다 — `🌤️ 33°...` → `🌤️ 오늘 33°...`.

- [ ] **Step 3: renderTimeline dayhead에 예보 줄**

`renderTimeline` 안 dayhead 부분:

변경 전:
```js
      '<div class="dtheme">' + day.theme.replace(/\n/g, ' · ') + '</div></div>' +
```

변경 후:
```js
      '<div class="dtheme">' + day.theme.replace(/\n/g, ' · ') + '</div>' +
      (function () {
        var line = wxLine(wxState.map, day.date);
        return line ? '<div class="dwx">' + line + wxStamp() + '</div>' : '';
      })() + '</div>' +
```

- [ ] **Step 4: selectDay에서 현재 일차 추적, init에서 wxRefresh 호출**

`selectDay` 첫 줄에 추가:

변경 전:
```js
function selectDay(n) {
  const days = window.TRIP.days;
```
변경 후:
```js
function selectDay(n) {
  currentDayN = n;
  const days = window.TRIP.days;
```

init(DOMContentLoaded) 마지막 `renderSpend();` 다음에 추가:

```js
  renderSpend();
  wxRefresh();
});
```

- [ ] **Step 5: 스타일**

`styles.css` 끝에 추가:

```css
#summary .wx { font-size:.9rem; opacity:.95; margin-top:2px; }
.dayhead .dwx { font-size:.85rem; color:var(--muted); margin-top:2px; }
.wxstamp { font-size:.75rem; opacity:.75; }
```

- [ ] **Step 6: Node 스텁 검증**

```bash
node -e "
global.localStorage={getItem:()=>null,setItem:()=>{}};
global.document={addEventListener:()=>{}};
eval(require('fs').readFileSync('app.js','utf8'));
console.log('syntax ok', typeof wxRefresh, typeof wxStamp);
// wxStamp: live=false, at 설정 시 스탬프 포함 확인
wxState.at='2026-07-27T08:30:00'; wxState.live=false;
console.log('stamp:', wxStamp().indexOf('기준') > -1 ? 'pass' : 'FAIL');
wxState.live=true;
console.log('no stamp when live:', wxStamp() === '' ? 'pass' : 'FAIL');
"
```
Expected: `syntax ok function function`, `stamp: pass`, `no stamp when live: pass`

브라우저 확인(온라인 실 API 응답, 오프라인 스탬프)은 컨트롤러가 수행.

- [ ] **Step 7: 커밋**

```bash
git add app.js styles.css
git commit -m "feat: 상단·일차별 날씨 표시 (Open-Meteo, 오프라인 캐시)"
```

---

### Task 3: sw.js same-origin 가드 + v7 + 번들

**Files:**
- Modify: `C:\Pcall\Trip\sw.js`
- Regenerate: `C:\Pcall\Trip\osaka-trip.html`

**Interfaces:**
- Consumes: 없음
- Produces: 없음 (최종)

- [ ] **Step 1: sw.js 수정**

1행: `const CACHE = "osaka-trip-v6";` → `const CACHE = "osaka-trip-v7";`

fetch 핸들러: cross-origin 응답(api.open-meteo.com)이 캐시에 쌓이지 않게 가드.

변경 전:
```js
self.addEventListener("fetch", function (e) {
  if (e.request.method !== "GET") return;
  e.respondWith(
    fetch(e.request).then(function (res) {
      const copy = res.clone();
      caches.open(CACHE).then(function (c) { c.put(e.request, copy); });
      return res;
    }).catch(function () {
```

변경 후:
```js
self.addEventListener("fetch", function (e) {
  if (e.request.method !== "GET") return;
  if (new URL(e.request.url).origin !== self.location.origin) return;
  e.respondWith(
    fetch(e.request).then(function (res) {
      const copy = res.clone();
      caches.open(CACHE).then(function (c) { c.put(e.request, copy); });
      return res;
    }).catch(function () {
```

(cross-origin은 respondWith 없이 브라우저 기본 처리에 맡긴다 — 날씨 오프라인 폴백은 앱 레벨 localStorage가 담당하므로 sw가 개입할 필요 없음)

- [ ] **Step 2: 번들 재생성 + 확인**

```bash
python bundle.py
grep -c "wxRefresh" osaka-trip.html
```
Expected: `wrote ...`, grep 결과 2 이상

- [ ] **Step 3: 커밋**

```bash
git add sw.js osaka-trip.html
git commit -m "chore: sw v7 — cross-origin 캐시 제외, 번들 재생성"
```

push는 컨트롤러가 최종 리뷰 후 수행.

---

## Self-Review

**Spec 커버리지:** API URL·좌표·타임존(Task 1 WX_URL), 코드 매핑(Task 1 wxIcon), 포맷·강수 null·없는 날짜 생략(Task 1 wxLine), 상단 오늘 줄(Task 2 Step 2), 일차별 줄(Task 2 Step 3), 캐시 저장·스탬프·비동기 재렌더(Task 2 Step 1), 캐시 없으면 미표시(wxLine이 빈 문자열 → 삼항으로 생략), sw v7 + cross-origin 가드(Task 3), data.js 무변경 — 전부 매핑됨.

**Placeholder:** 없음. 모든 코드 단계에 실제 코드.

**타입 일관성:** wxState/wxStamp/wxRefresh/wxRepaint/currentDayN 명칭이 정의부(Task 2 Step 1)와 사용부(Step 2-4)에서 일치. wxLine(map,date) 시그니처 Task 1↔2 일치.

**참고:** renderSummary의 replace(" ", " 오늘 ")는 이모지에 공백이 포함되지 않는 전제 — wxIcon의 이모지는 전부 단일 그래핌이며 자체 공백 없음, 첫 공백은 항상 이모지 직후이므로 안전.
